/**
 * Marker base class for all protocol-attribute entities.
 * Add a new protocol by creating a class that extends this one,
 * then register it in the PROTOCOL_ATTRIBUTES_MAP.
 */
export abstract class BaseProtocolAttributesEntity {}
