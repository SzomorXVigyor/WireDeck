# WireGuard Configuration
INIT_USERNAME=admin
INIT_PASSWORD=your-secure-password
INIT_HOST=localhost

# Application Settings
NODE_ENV=development
PORT=3000

# Optional: Override server host for admin panel links
# SERVER_HOST=your-docker-host-ip